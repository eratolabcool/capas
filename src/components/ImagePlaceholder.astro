---
export interface Props {
  src?: string;
  alt: string;
  className?: string;
}

const { src = '/images/placeholder-cover.jpg', alt, className = '' } = Astro.props;
---

<img src={src} alt={alt} loading="lazy" decoding="async" class={className} />
