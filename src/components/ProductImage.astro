---
interface Props {
  src: string;
  alt: string;
}

const { src, alt } = Astro.props;
---

<img src={src} alt={alt} loading="lazy" decoding="async" />

<style>
  img {
    width: 100%;
    height: auto;
    border-radius: 16px;
    object-fit: cover;
  }
</style>
